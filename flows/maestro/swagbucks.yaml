appId: ${SWAGBUCKS_APP_ID}
---
- launchApp
- waitForAnimationToEnd
- takeScreenshot: ${SCREENSHOT_DIR}/swagbucks-launch.png
# Add confirmed UG entry steps only after first manual route discovery.
# Do not automate survey answers, gift-card redemption, identity, or account verification.
